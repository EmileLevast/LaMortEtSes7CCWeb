# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Java CI with Gradle

#pour declencher un build git tag --delete v1.0.0 && git push origin :v1.0.0 && git tag v1.0.0 && git push origin --tags
on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

jobs:
  build:
    name: Create Release
    runs-on: windows-latest
    permissions: write-all

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 20
      uses: actions/setup-java@v4
      with:
        java-version: '20'
        distribution: 'temurin'

    # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
    # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
    - name: Setup Gradle 8.11.1
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '8.11.1' # Quotes required to prevent YAML converting to number
    
    - name: Build Desktop with Gradle
      run: gradle packageReleaseMsi

    - name: Build Apk with Gradle
      run: gradle assembleDebug

    - name: Build FatJar Ktor with Gradle
      run: gradle buildFatJar

    - name: Create & Publish Release with Assets
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        name: Release ${{ github.ref }}
        draft: false
        prerelease: false
        files: |
          ./composeApp/build/compose/binaries/main-release/msi/LesFeusPeregrins-1.0.0.msi
          ./composeApp/build/outputs/apk/debug/composeApp-debug.apk
          ./server/build/libs/server-all.jar

    - name: Publish release
      uses: StuYarrow/publish-release@v1.1.2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        id: ${{ steps.create_release.outputs.id }}

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - uses: actions/checkout@v4

    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: server-all.jar

      # --- Deployment to EC2 ---
    - name: Upload FatJar to EC2
      uses: appleboy/scp-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        source: "./server/build/libs/server-all.jar"
        target: "/home/ec2-user/files/server-all.jar"

    - name: Restart service on EC2
      uses: appleboy/ssh-action@v1.2.5
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          # Donner les droits d'exécution au nouveau JAR
          sudo chmod 755 /home/ec2-user/files/server-all.jar
          sudo systemctl stop lesfeusperegrins
          sudo systemctl restart lesfeusperegrins
